framework:
    validation:
        email_validation_mode: html5

        # Enables validator auto-mapping support.
        # For instance, basic validation constraints will be inferred from Doctrine's metadata.
        #auto_mapping:
        #    App\Entity\: []

when@test:
    framework:
        validation:
            not_compromised_password: false


#propreties:
    #nom:
        #-length:
         #   min: 2
          #  max: 5
           # minMessage: 'le nom de votre categorie est trop petit {{ limit }} '
            #maxMessage: 'le nom de votre categorie est trop grand {{ limit }}'